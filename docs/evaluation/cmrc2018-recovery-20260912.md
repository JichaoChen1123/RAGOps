## CMRC2018 数据材料恢复记录

恢复日期：2026-09-12

本次只恢复数据准备材料；未抽样、未调用模型、未创建评测任务，也未修改评分、Prompt 或产品功能。

### 已恢复材料

- 官方 dev 原始数据已下载至 `W:\RAGOPS\RAGOps\data\CMRC2018\cmrc2018_dev.json`。
  - 官方仓库：[ymcui/cmrc2018](https://github.com/ymcui/cmrc2018)
  - 具体源地址：`https://raw.githubusercontent.com/ymcui/cmrc2018/master/squad-style-data/cmrc2018_dev.json`
  - 已验证为 JSON 数据（顶层字段 `version`、`data`；`data` 含 848 篇文章），不是 HTML 或错误页。
- 旧 JSONL 不在指定工作区或 Git stash 中。已从备份数据库副本只读导出历史任务 `01a07f6f-2d23-7121-b972-af56ebeb972f` 的 10 条关联样本至 `W:\RAGOPS\recovery\cmrc2018-materials-20260912\cmrc2018_legacy_job_01a07f6f_samples.jsonl`。
  - 每条仅包含 `sample_id`、`source_id`、`question`、`reference_answer` 与完整 `contexts`。
  - `reference_answer` 仅取自 `dataset_samples.reference_answer`；未导出 `answer`、`historical_answer` 或评测任务回答。
- 备份原件 `W:\RAGOPS\backups\before-pr37-20260911-225747\data\ragops.db` 未改动；只复制其副本至 `W:\RAGOPS\recovery\cmrc2018-materials-20260912\ragops.db` 并以 SQLite 只读模式查询。

### 未恢复项与后续缺口

- `scripts/convert_cmrc2018.py` 和 `squad-style-data/cmrc2018_dev_ragops_v2.jsonl` 在两处指定位置均不存在。
- `W:\RAGOPS\RAGOps` 的 `stash@{0}` 与 `stash@{1}` 文件清单均不含上述目标文件；`W:\简历优化\RAGOps` 不是 Git 仓库。
- 旧 10 条样本已完整恢复为独立 JSONL，但原来的 v2 JSONL 文件本身未找回。下一轮如需重建转换脚本，应遵循现有数据契约，且不能将本次恢复的模型回答作为参考答案。

原始数据、数据库副本和恢复 JSONL 均未提交到仓库。
